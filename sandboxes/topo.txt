# version 1

-r ../modules/columnflow/sandboxes/columnar.txt

# The TOPO trigger-efficiency ensemble is transported as XGBoost booster JSON (see
# hbw/production/topo_trigger.py), so the evaluating sandbox needs xgboost.
#
# PINNED, not floating. The exported boosters were fitted with 2.1.3, and a pin is what turns
# "the tree format happens to still load" into a guarantee. Bump it only together with a
# re-export and a re-run of the round-trip check.
#
# This deliberately does NOT reuse sandboxes/onnx.txt: that venv is shared with the DL-facing
# nlo_reweighting and transformer producers, and a version bump there forces a rebuild on
# everyone for a dependency they do not use.
xgboost==2.1.3
